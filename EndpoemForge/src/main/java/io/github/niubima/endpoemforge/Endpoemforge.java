package io.github.niubima.endpoemforge;

import net.minecraftforge.fml.common.Mod;

@Mod(Endpoemforge.MODID)
public class Endpoemforge {
    public static final String MODID = "endpoemforge";
    public Endpoemforge() {
        // 不要 register(this) / 不要 addListener，这里留空即可
    }
}
